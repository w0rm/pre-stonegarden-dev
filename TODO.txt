
1. Cut/copy/paste on documents

2. Edit page code dialog

3. Render the following forms in templates by parts:
	user forms
	login form
	password reset form
	password change form


INSERT INTO `blocks` (`user_id`, `level`, `name`, `position`, `template`, `size`, `is_published`) VALUES ('1', '0', 'news_contact', '1', 'column', '12', '1');
ALTER TABLE `pages` ADD COLUMN `params` INTEGER  DEFAULT '0'  AFTER `path`;
UPDATE pages SET published_at = CURRENT_TIMESTAMP WHERE is_published;


UPDATE `pages` SET `params`='1' WHERE `type`='news_archive';


<% if (!page.id) { %>
    <p>
        <label>$_("Page type")</label>
        <select name="type">
        $for page in config.page_types.keys():
            <option value="$page">$page</option>
        </select>
    </p>
<% };%>
